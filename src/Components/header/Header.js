import React from 'react';
import HeaderList from './headerList/HeaderList';

const Header = () => {
    return (
        <div>
            <HeaderList/>
        </div>
    );
};

export default Header;